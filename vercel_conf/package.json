{
  "name": "cashdash",
  "version": "0.0.1",
  "description": "lorem ipsum",
  "main": "index.js",
  "scripts": {
    "build-frontend": "cd ../frontend && cd ../vercel_conf",
    "build-backend": "cd ../backend && npm install && cd ../vercel_conf",
    "build-vercel": "npm install",
    "build": "npm run build-frontend && npm run build-backend && npm run build-vercel",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.50.3",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^17.1.0",
    "express": "^5.1.0"
  },
  "author": "Pixelcubes",
  "license": "ISC"
}
